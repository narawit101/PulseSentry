# Standalone Lightweight Desktop Window using WebView2

We decided to build the application as a standalone desktop application (.exe) using Windows native WebView2 instead of a pure browser tab or heavy Electron wrapper, in order to provide a dedicated, zero-friction desktop experience with minimal memory footprint (~40-60MB).
